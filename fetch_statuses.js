const { createClient } = require('@supabase/supabase-js'); const fs = require('fs'); const env = fs.readFileSync('.env', 'utf8'); const urlMatch = env.match(/VITE_SUPABASE_URL=(.*)/); const keyMatch = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/); const supabase = createClient(urlMatch[1], keyMatch[1]); async function run() { const { data, error } = await supabase.from('invoices').select('status'); if (error) console.error(error); else { const statuses = new Set(data.map(d => d.status)); console.log(Array.from(statuses)); } } run();